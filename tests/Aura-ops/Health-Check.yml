info:
  name: Health-Check
  type: http
  seq: 2

http:
  method: GET
  url: https://cf_ai_aura-ops.sakshamgupta-ai.workers.dev/health
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "level" : "error",
        "message" : "Redis Connection Timeout"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
