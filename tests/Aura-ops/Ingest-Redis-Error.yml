info:
  name: Ingest-Redis-Error
  type: http
  seq: 4

http:
  method: POST
  url: https://cf_ai_aura-ops.sakshamgupta-ai.workers.dev/ingest/auth-api
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "level" : "error",
        "message" : "Redis Connection Timeout"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
